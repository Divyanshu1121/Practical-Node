<% include('layout', { title: 'My Articles' }); %>
    <h1>My Articles</h1>
    <ul>
        <% articles.forEach(article=> { %>
            <li>
                <h2><a href="#">
                        <%= article.title %>
                    </a></h2>
                <p>
                    <%= article.content.substring(0, 100) %>...
                </p>
            </li>
            <% }); %>
    </ul>